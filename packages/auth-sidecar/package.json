{
  "name": "@tinker/auth-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/auth-sidecar.mjs",
  "types": "./src/index.ts",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/auth-sidecar.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "better-auth": "^1.6.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
