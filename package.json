{
  "name": "ramp-glass",
  "private": true,
  "version": "0.0.0",
  "description": "Local-first AI workspace — 1:1 clone of Ramp's internal Glass suite.",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" --filter \"./apps/*\" build",
    "typecheck": "pnpm exec tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2023,DOM,DOM.Iterable --strict --noUncheckedIndexedAccess --exactOptionalPropertyTypes --noImplicitOverride --noFallthroughCasesInSwitch --resolveJsonModule --esModuleInterop --forceConsistentCasingInFileNames --isolatedModules --skipLibCheck --allowSyntheticDefaultImports --verbatimModuleSyntax --types node $(find packages/shared-types/src packages/glass-bridge/src packages/memory/src apps/desktop/src/main apps/desktop/src/preload -type f \\( -name '*.ts' -o -name '*.tsx' \\) | sort)",
    "lint": "pnpm -r --parallel lint",
    "test": "pnpm -r --parallel test",
    "dev:desktop": "pnpm --filter @ramp-glass/desktop dev",
    "dev:dojo-web": "pnpm --filter @ramp-glass/dojo-web dev",
    "clean": "pnpm -r exec rm -rf dist out .turbo node_modules && rm -rf node_modules"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.4.3",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "front-matter": "^4.0.2",
    "keytar": "^7.9.0",
    "opencode-ai": "^1.4.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.19.39",
    "@vitest/ui": "^2.1.9",
    "electron-playwright-helpers": "^2.1.0",
    "playwright": "^1.59.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
