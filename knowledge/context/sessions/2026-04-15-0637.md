# Session 2026-04-15-0637 — Feature 01 SSO Connector Layer

Contributor: Codex
Branch: `codex/01-sso-connector-layer`

## Scope

Finished feature 01 on branch: Google + GitHub sign-in, keychain-backed provider session restore, GitHub MCP activation through OpenCode restart, and first-run/settings/workspace integration status UI.

## Changes

- Added `apps/desktop/src-tauri/src/commands/auth.rs` with:
  - `auth_sign_in(provider)`
  - `auth_sign_out(provider)`
  - `auth_status()`
  - Google loopback OAuth
  - GitHub device flow
  - system keychain session storage
- Updated `apps/desktop/src-tauri/src/lib.rs` so OpenCode can restart and inherit `TINKER_GITHUB_TOKEN` from stored GitHub session.
- Replaced renderer-side keyring handling with Tauri auth commands in `apps/desktop/src/renderer/App.tsx`.
- Added `apps/desktop/src/renderer/components/IntegrationsStrip.tsx` and wired it into FirstRun, Settings, and Workspace header flow.
- Added GitHub MCP server entry to `opencode.json`.
- Expanded shared SSO types to cover both `google` and `github`.

## Decisions Made

- GitHub uses device flow, not web callback flow. Reason: desktop app should not require a shipped `client_secret`.
- GitHub MCP token is injected at OpenCode process start, so sign-in/sign-out restarts sidecar. Google stays on OpenCode auth provider plumbing.
- Renderer owns connector sync state and status display. Rust stays at auth + sidecar plumbing boundary.

## Knowledge Base Updates

- `knowledge/context/tasks.md` → feature 01 set to `review`
- `knowledge/features/01-sso-connector-layer.md` → implementation notes updated, acceptance criteria ticked

## Verification

- `cargo check` from `apps/desktop/src-tauri` → pass
- `pnpm --filter @tinker/desktop typecheck` → pass before unrelated workspace pipeline files re-entered root check
- `pnpm test` → pass in current worktree

## Open Questions

- Full live OAuth verification still needs local env vars:
  - `GOOGLE_OAUTH_CLIENT_ID`
  - `GITHUB_OAUTH_CLIENT_ID`
- Google MCP package runtime behavior still needs manual app run confirmation.

## Gaps Surfaced

- Repo currently has unrelated in-progress memory pipeline work outside feature 01 (`packages/memory`, `apps/desktop/src/renderer/memory.ts`, `Chat.tsx`, `Today.tsx`, `packages/bridge`). Root `pnpm typecheck` now fails on that separate work. Feature 01 files themselves were reviewed against current branch diff and kept isolated from those edits.
