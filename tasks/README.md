# Task Notes

The previous task briefs were tied to the initial scaffold and were removed during the Tinker rename and Tauri migration.

Use these files instead:

- `tinker-prd.md` for product and architecture intent
- `CLAUDE.md` / `AGENTS.md` for implementation rules
