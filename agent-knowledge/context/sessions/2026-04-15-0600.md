# Session 2026-04-15 — Native scheduler implementation

Contributor: @khvni
Agent: Codex (GPT-5)

## Scope
- Implemented [[04-native-scheduler]] end-to-end across shared types, SQLite persistence, scheduler runtime, desktop UI, and knowledge-base status updates.
- Addressed task `04 | Native scheduler (cron-style scheduled prompts)` from [[tasks]].

## Changes
- Added scheduler domain types in `packages/shared-types/src/scheduler.ts` and exposed new `scheduler` pane kind.
- Added SQLite tables for `jobs` and `job_runs`, plus `createScheduledJobStore()` in `packages/memory/src/scheduler-store.ts`.
- Added new `packages/scheduler` package with cron parsing, next-run previewing, missed-run skip counting, OpenCode prompt execution, sink delivery, and background polling engine.
- Added desktop notification support through `@tauri-apps/plugin-notification` and thin Tauri plugin registration in Rust.
- Added `SchedulerPane` UI in `apps/desktop/src/renderer/panes/SchedulerPane.tsx` with create/edit, enable/disable, delete, run-now, and per-job history.
- Updated `Today` pane to show recent scheduled outputs routed to the Today sink.
- Fixed an existing Rust ownership bug in `src-tauri/src/commands/auth.rs` while validating `cargo check`.

## Decisions Made
- [2026-04-15] Catch-up behavior resolved in favor of acceptance criteria: missed runs are skipped, not replayed, and one skipped-history row records the skipped count.
- [2026-04-15] Desktop notifications required a small Rust plugin registration even though scheduler logic remains in TypeScript; treated as allowed system plumbing, not business logic.

## Knowledge Base Updates
- Updated `knowledge/features/04-native-scheduler.md` status to `review`.
- Clarified catch-up semantics in `knowledge/features/04-native-scheduler.md`.
- Updated `knowledge/context/tasks.md` status for feature 04 to `review`.

## Open Questions / Handoff
- No new product-scope blockers found.
- UI has natural-language parsing for common phrases (`daily at 8am`, `every weekday at 8am`, `monday at 9am`) plus direct cron/alias entry. If product wants broader NL coverage, expand parser in `packages/scheduler/src/schedule.ts`.

## Gaps Surfaced
- Feature spec originally contradicted itself on catch-up behavior (`run on next launch` vs `skip missed runs`). Acceptance criteria won; spec now reflects implemented behavior.
- No app-level integration tests exist yet for background polling + sink delivery. Current verification is unit tests, TypeScript typecheck, and `cargo check`.
