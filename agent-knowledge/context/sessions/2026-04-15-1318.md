# Session 2026-04-15 — Desktop hardening follow-through

Contributor: @khvni (via Codex + parallel workers)
Agent: GPT-5

## Scope

User asked for `knowledge/context/sessions/2026-04-15-1344.md`, but that file did not exist in this worktree on 2026-04-15. Follow-up tasks were derived from the latest workspace hardening handoff in `knowledge/context/sessions/2026-04-15-0548.md`, then executed in parallel.

## Changes

### Workspace follow-ups
- Added persistent workspace preference `autoOpenAgentWrittenFiles`, default on, stored inside the existing layout payload.
- Added Settings toggle for that preference.
- Added explicit multi-chat support with stable `chat` / `chat-N` panel IDs and new-chat actions in both workspace header and chat pane.
- Extracted pure workspace helpers for file-open reuse/new-panel logic and chat-panel ID allocation.
- Added regression tests for layout preference round-trip, legacy fallback, file-open reuse/new-panel, and chat-panel ID behavior.

### Renderer + bridge hardening
- Replaced plain code-pane fallback with lazy syntax highlighting via `highlight.js`, while keeping large files on plain-text fallback for responsiveness.
- Added bridge stream tests that prove `file_written` events emit for both patch parts and file parts.

### Build / run path
- Added project-root `script/build_and_run.sh`.
- Added `.codex/environments/environment.toml` Run action pointing at that script.
- Local verify run succeeded through `./script/build_and_run.sh --verify`, including Tauri bundle build and app launch.

## Verification

- `pnpm typecheck`
- `pnpm test`
- `pnpm lint`
- `bash -n script/build_and_run.sh`
- `./script/build_and_run.sh --verify`

## Remaining risks

- No true end-to-end Dockview relaunch test yet; coverage stays unit-level.
- Code pane has helper-level tests, not DOM-level render tests.
- Build script currently uses Tauri bundle build path, so verify remains slower than a dev-run loop.
