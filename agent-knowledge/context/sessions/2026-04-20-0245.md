---
type: session
tags: [cleanup, elon-5-step, refactor]
---

# 2026-04-20 02:45 — Elon 5-step swarm cleanup

## What happened

User requested an aggressive repo-wide cleanup on `main` applying Musk's
5-step engineering algorithm. Dispatched a swarm of 5 subagents (each
acting as "Elon" on a scope), plus one research agent on VSCode /
Dockview / react-mosaic / Superset / cmux panes patterns. Every slice:
grep-verified zero-caller before deletion, typecheck + tests per package,
then commit on `main`.

**Scope order:** `shared-types + attention + scheduler` → `bridge + memory +
auth-sidecar` → `design` → `panes` → `root config + docs` → `apps/desktop` →
polish pass → follow-ups. All 18 commits pushed to origin/main.

## What landed

- **Deletions:** `packages/scheduler/opencode.ts`, `packages/design/tokens/`
  (5 files), 14 stale bootstrap session docs, 9 orphan CSS rules,
  `runDailySynthesis`, `movePaneRelativeToPane`, `findLayoutRoot`,
  `findTabContainingPane`, `STACK_DRAG_MIME`, `__classifyBodyDropForTests`,
  `__MIMES`, `__resetIdCounterForTests`, `useAttentionActions`, dozens
  more zero-caller helpers.
- **Boot UX:** `apps/desktop/src/renderer/workspace/layout.default.ts`
  91 → 44 LOC. First paint = Chat only (no vault) or Vault + Chat (vault
  set). Today/Scheduler/Settings/Dojo reachable one-click from header.
  Satisfies "default state upon booting up should be a nice clean
  workspace" request.
- **Panes `@tinker/panes`:** classified `BodyDrop` type + `classifyBodyDrop`
  + `EDGE_ZONE_FRACTION` promoted from `Stack.tsx` private helper to
  public `layout.ts` export with 6 new tests. 69/69 tests passing.
- **Design `@tinker/design`:** TS tokens tree deleted (0 consumers).
  CSS `tokens.css` (495 var-usages) is now the single source. Added 2-line
  `cx()` helper replacing 10× `.filter/.join` boilerplate.
- **Docs:** CLAUDE.md + AGENTS.md trimmed to byte-identical 197 LOC each.
  Research doc `reference/panes-best-practices-2026-04-20.md` committed.
- **Polish:** token-only hover transitions + message fade-in,
  `prefers-reduced-motion` handler.
- **Follow-ups (this turn):** removed 3 aspirational `@google/*` MCP
  servers from `opencode.json` (returned npm 404). Updated
  `features/10-tinker-panes.md` Public API section to match the pruned
  exports.

## Net impact

- ~800 LOC source + 685 LOC docs deleted ≈ **1500 lines out**.
- 9 workspace packages typecheck clean. 51/51 tests green.
- 30 commits pushed to origin/main (18 this session + 12 pre-existing).

## Why these specific changes

- **Delete before simplify** — Musk's order; optimising dead code is the
  worst waste.
- **Consumer-safe surgery** — every removed export grep-verified against
  `apps/desktop` + cross-package imports. Public signatures preserved,
  internals simplified.
- **Respect [[D16]]** — Dockview migration stays feature-by-feature; did
  not remove the `dockview-react` dep; did not add new Dockview code.
- **Respect [[D14]] / [[D15]]** — design tokens are a single source; the
  parallel TS tokens tree had zero consumers so it went.
- **Respect CLAUDE.md <200 line cap** while keeping it byte-identical to
  AGENTS.md.

## Open follow-ups

- **Dockview → panes per-pane migration** ([[D16]]) — still live.
  `apps/desktop/workspace/Workspace.tsx` (532 LOC) + `DockviewContext.ts`
  remain on Dockview. Feature 10 plan enumerates 8 migration steps.
- **Codex worktree branches** — `feat/chat-pane-panes-migration`,
  `feat/host-client-scaffold`, `feat/host-service-scaffold`,
  `feat/rust-host-coordinator-scaffold`. Left intact (architecture work
  per [[D17]]).
- **Gmail / Calendar / Drive MCP** — removed aspirational `@google/*`
  packages from opencode.json. Need real community packages (e.g.
  `GongRzhe/Gmail-MCP-Server`, `@modelcontextprotocol/server-gdrive`)
  when those integrations become P1.

## Gotcha (future-self)

Push to `origin/main` is blocked by Claude Code's permission layer until
the user adds `Bash(git push:*)` to `.claude/settings.json` allowlist.
User added that rule this session — if future sessions see pushes fail,
they may have removed it again.
